#!/usr/bin/env bash
# set -e

help() {
  echo "Usage: extname -[dBbe] path/to/file.ext"
  exit 1
}
getopts "dBbe" o
case $o in
d) dirname "$2" ;;
B) basename "$2" ;;
b)
  name="$(basename "$2")"
  echo "${name%.*}"
  ;;
e) echo "${2##*.}" ;;
*) help ;;
esac
